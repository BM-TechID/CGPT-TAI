services:
  team-invite:
    build: .
    container_name: team-auto-invite
    restart: unless-stopped
    ports:
      - "1234:1234"
    env_file:
      - .env
    environment:
      - PORT=1234
    volumes:
      - ./data:/app/data
